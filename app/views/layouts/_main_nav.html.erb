<div id="main-nav" class="">
  <div class="container">
    <div class="nav-header">
      <a class="nav-brand" href="index.html"><i class="icon-lime"></i>Key Lime</a>
      <a class="menu-link nav-icon" href="#"><i class="icon-menu2"></i></a>
      <% if user_signed_in? %>
          <%= link_to 'Выйти', destroy_user_session_path, method: :delete, class: 'btn btn-blog outline-white pull-right' %>
          <div class="welcome">Привет, <%= link_to current_user.login, current_user %></div>
      <% else %>
          <a class="btn btn-blog outline-white pull-right" href="#" data-toggle="modal" data-target="#loginModal">Войти</a>
          <a class="btn btn-blog outline-white pull-right" href="#" data-toggle="modal" data-target="#registerModal">Регистрация</a>
      <% end %>
    </div>
  </div>
</div>